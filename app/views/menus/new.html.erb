<h1 class="text-3xl text-gray-700">メニュー登録</h1>
<% if @menu.errors.any? %>
  <ul>
    <% @menu.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form', menu: @menu %>
